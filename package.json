{
  "name": "shopify-trello-bridge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.106.1",
    "@types/aws-lambda": "^8.10.70",
    "prettier": "^2.3.0"
  },
  "dependencies": {
    "@aws-cdk/core": "1.106.1",
    "@serverless-stack/cli": "0.23.0",
    "@serverless-stack/resources": "0.23.0",
    "trello-node-api": "^0.0.9"
  }
}
